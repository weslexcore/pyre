---
interface Props {
  class?: string;
  action?: string;
  method?: "GET" | "POST";
}

const { 
  class: className = "", 
  action = "#", 
  method = "POST" 
} = Astro.props;
---

<form 
  action={action} 
  method={method}
  class={`space-y-4 ${className}`}
>
  <slot />
</form> 