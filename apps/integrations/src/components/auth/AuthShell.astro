---
// The page frame the sign-in screens share (login, set-password, forgot
// password): no nav, one narrow column, never cached or indexed.
import Favicon from '@/components/Favicon.astro';
import '@/styles/admin.css';

interface Props {
  title?: string;
}

const { title = 'Pyre Admin' } = Astro.props;

Astro.response.headers.set('Cache-Control', 'private, no-store');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"
    />
    <meta name="robots" content="noindex, nofollow" />
    <title>{title}</title>
    <Favicon />
  </head>
  <body class="min-h-screen bg-[var(--pyre-black)] font-sans text-[var(--pyre-creme)] antialiased">
    <main class="mx-auto max-w-md px-4 py-16 text-center">
      <slot />
    </main>
  </body>
</html>

