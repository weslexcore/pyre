---
// The goals index. Anyone granted /admin/goals lands here; the
// /api/admin/goals route re-checks the session on every request, so this
// page's gate is UX.
import { GoalsIndex } from '@/components/admin/goals/GoalsIndex';
import AdminLayout from '@/layouts/AdminLayout.astro';
---

<AdminLayout
  title="Goals"
  subtitle="What we are trying to achieve, the numbers that say whether we got there, and the work that adds up to it"
>
  <GoalsIndex client:load />
</AdminLayout>
