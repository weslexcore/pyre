---
// "New goal" from the global search. The form lives on the index — writing a
// goal down next to the ones already there is the right context for it — so
// this route exists only to open it.
return Astro.redirect('/admin/goals?new=1');
---
