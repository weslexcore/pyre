import type { BookingContent } from './types';

const booking: BookingContent = {
  label: 'Book',
  bookingBaseUrl: 'https://www.sweatpals.com/host/pyre',
  utmSource: 'landing',
  ariaLabel: 'Book now',
};

export default booking;
