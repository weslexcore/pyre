---
import { Image } from 'astro:assets';
import { getDividerVariant } from '@/lib/divider-utils';
import { formatReadingTime } from '@/lib/reading-time';
import type { BlogPostCard } from '@/lib/types';
import SectionDivider from '../SectionDivider.astro';

interface Props {
  post: BlogPostCard;
}

const { post } = Astro.props;
const formattedDate = post.date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
  timeZone: 'UTC',
});
const dividerVariant = getDividerVariant(post.slug);
---

<article class="group relative flex flex-col md:flex-row overflow-hidden rounded-[var(--radius)] bg-[var(--pyre-black)] text-[var(--pyre-creme)] transition-all duration-300 shadow-2xl hover:shadow-[0_8px_30px_rgb(0,0,0,0.12)] cursor-pointer">
  <!-- Featured Image -->
  {post.image ? (
    <div class="relative aspect-[16/9] md:aspect-auto md:w-2/5 overflow-hidden shrink-0">
      {typeof post.image === 'string' ? (
        <img
          src={post.image}
          alt={post.title}
          class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
          loading="lazy"
        />
      ) : (
        <Image
          src={post.image}
          alt={post.title}
          width={800}
          height={450}
          class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
          loading="lazy"
        />
      )}
    </div>
  ) : (
    <div class="relative aspect-[16/9] md:aspect-auto md:w-2/5 bg-gradient-to-br from-secondary to-primary shrink-0">
      <div class="flex h-full items-center justify-center p-6">
        <span class="font-sans text-2xl md:text-3xl font-semibold tracking-[-0.02em] text-[var(--pyre-creme)] text-center">
          {post.title}
        </span>
      </div>
    </div>
  )}

  <!-- Content -->
  <div class="flex flex-1 flex-col p-6 md:p-8">
    <!-- Title -->
    <h3 class="mb-3">
      <a
        href={`/blog/${post.slug}`}
        class="font-sans text-xl md:text-3xl font-semibold leading-tight tracking-[-0.02em] text-[var(--pyre-creme)] transition-colors group-hover:text-[var(--pyre-red)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-secondary focus-visible:ring-offset-2 after:absolute after:inset-0 after:content-['']"
      >
        {post.title}
      </a>
    </h3>

    <!-- Description -->
    <p class="mb-4 flex-1 font-sans text-base md:text-lg leading-relaxed text-[var(--pyre-creme)]">
      {post.description}
    </p>
    <SectionDivider
      variant={dividerVariant}
      revealAnimation="draw"
      visibleAnimation="pulse"
      class="mx-auto w-full my-1 h-0.5"
      scrollAware={true}
      strokeWidth={1.5}
    />


    <!-- Meta info -->
    <div class="flex flex-wrap items-center gap-4 pt-4 font-mono text-sm text-[var(--pyre-creme)]/70">
      <span class="font-bold">{post.author}</span>
      <!-- <span aria-hidden="true">•</span> -->
      <!-- <time datetime={post.date.toISOString()}>{formattedDate}</time> -->
      <span aria-hidden="true">•</span>
      <span>{formatReadingTime(post.readingTime)}</span>
    </div>

    <!-- Tags -->
    <div class="mt-4 flex flex-wrap gap-2">
      {post.tags.map((tag) => (
        <a
          href={`/blog?tag=${encodeURIComponent(tag)}`}
          class="relative z-10 rounded-md bg-secondary px-3 py-1 font-mono text-xs font-bold uppercase tracking-wide text-background transition-colors hover:bg-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-secondary focus-visible:ring-offset-2"
        >
          {tag}
        </a>
      ))}
    </div>
  </div>
</article>

