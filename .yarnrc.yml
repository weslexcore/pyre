approvedGitRepositories:
  - "**"

enableScripts: true

nodeLinker: node-modules

# Only install npm package versions published at least 14 days ago
# (supply-chain protection against freshly compromised releases)
npmMinimalAgeGate: 20160
