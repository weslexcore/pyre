{
  "name": "@pyre/signals-core",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "description": "The shared vocabulary for classifying free text with Jev (shift notes first): what it detects (signal types), what it can read (subject types), the questions put to Jev, and the rules for keeping its answers.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@pyre/jev": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "ai": "7.0.105",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "zod": "^4.4.3"
  }
}
