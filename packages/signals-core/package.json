{
  "name": "@pyre/signals-core",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "description": "The shared vocabulary for AI classification of free text (shift notes first): what the classifier detects (signal types), what it can read (subject types), and the payload rules both the agents app and the integrations app enforce.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
